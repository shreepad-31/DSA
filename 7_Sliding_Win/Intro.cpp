#include <bits/stdc++.h>
using namespace std;

int max_sum(vector <int> arr, int k){
    int left = 0, right = k - 1, max_sum = 0, n = arr.size(), sub_arr;
    while(right < n){
        left++; right++;
    }
    return ;
}

int main(){

    // Constant Window
    // Maximum sum of a subarray with k consecutively elements

    vector<int> arr = {}; 
    int res, k = 4;
    res = max_sum(arr, k);


    // Longest subarray with {condition}
    // Longest subarray with sum <= k


    // Number of Subarrays (with some condtions)

    // Shortest Window

    return 0;
}